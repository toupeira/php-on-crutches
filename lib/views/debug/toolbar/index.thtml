<?# $Id$ ?>
<?
   $panels = array_merge(
      array('menu', 'request', 'log', 'template', 'database', 'headers', 'system')
   );
?>
<div id="debug-toolbar">
<? foreach ($panels as $panel): ?>
<div id="debug-toolbar-<?= $panel ?>"><?= $this->render_partial($panel) ?></div>
<? endforeach ?>
</div>
