<?# $Id$ ?>
<table class="scaffold-list">
<? foreach ($databases as $database => $models): ?>
<tr>
   <th colspan="2"><?= h($database) ?></th>
</tr>
<? foreach ($models as $model => $count): ?>
<tr class="<?= cycle() ?>">
   <td><?= image_tag('framework/icons/debug.png') ?> <?= link_to(h($model), ':/database/'.underscore($model)) ?></td>
   <td><?= pluralize($count, 'record', 'records') ?></td>
</tr>
<? endforeach ?>
<? endforeach ?>
</table>
