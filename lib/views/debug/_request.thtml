<?# $Id$ ?>
<?
   $request = array(
      'Path'       => h($_SERVER['REQUEST_URI']),
      'Method'     => $_SERVER['REQUEST_METHOD'],
      'Parameters' => dump_colored(Dispatcher::$params),
   );

   if ($_COOKIE) {
      $request['Cookies'] = dump_colored($_COOKIE);
   }

   if ($_SESSION) {
      $request['Session'] = dump_colored($_SESSION);
   }
?>
<span><a>Request: <?= h(Dispatcher::$path) ?></a></span>
<div><?= table_tag($request) ?></div>
